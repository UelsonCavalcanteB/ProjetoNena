unit uClasseGeral;

interface
//property
 // procedure HabilitaMenus(Formulario: TForm; Valor: Boolean);

implementation
//procedure HabilitaMenus(Formulario: TForm; Valor: Boolean);
//var
//  I: Integer;
{begin
  for I := 0 to Formulario.ComponentCount - 1 do
    begin
      if (Formulario.Components[i] is TSpeedButton) then
      (formulario.Components[i] as TSpeedButton).Enabled := Valor

    end; }
end.
